import React from 'react'

const ExecuteFunction = ({ showMessage }) => {
    return (
        <div>
            <button onClick={showMessage}> clique para testar function </button>
        </div>
    )
}

export default ExecuteFunction